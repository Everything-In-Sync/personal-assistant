# print(' \n Solidify Task?')
# userResponse = raw_input()
# if userResponse == 'yes':
# read each line of the file and covert it to an instance of Tasks then add the instance name to array
# with open("tasks.txt", "r") as solidifyTask: #you probably do not need to open the file again,m can just keep it open while the new task is being created
# elif userResponse == 'no':
#     print('Holding Task')
# elif query == 'merge tasks':
#     with open("tasks.txt", "r") as mergeTasksFile:
